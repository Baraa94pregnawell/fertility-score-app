[variables]
NODE_VERSION = "20.19.1"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["DATABASE_URL=${DATABASE_URL:-postgresql://build:build@localhost:5432/build} npm run build"]

[start]
cmd = "npm run start"
