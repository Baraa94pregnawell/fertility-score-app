[variables]
NODE_VERSION = "20.19.1"
DATABASE_URL = "postgresql://builduser:buildpass@localhost:5432/builddb"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
